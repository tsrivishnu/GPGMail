MACOS_VERSION=13
if [[ "$GIT_BRANCH" == "origin/mojave" ]]; then
	MACOS_VERSION=14
fi
TOOL=GPGMail_${MACOS_VERSION}
MAJOR=3
MINOR=0
REVISION=1
#PRERELEASE=b7

VERSION="${MAJOR}.${MINOR}${REVISION:+.$REVISION}${PRERELEASE}"
